package ${basePackageName}.coreclient.dto.${moduleName}.${allLowName};

import lombok.Data;
import javax.validation.constraints.NotNull;
import lombok.experimental.Accessors;
import ${basePackageName}.coreclient.dto.common.command.CommonCommand;
/**
 * ${comments}
 * @author ${author}
 * @date ${datetime}
 */
@Data
@Accessors(chain = true)
public class ${className}UpdateCmd extends CommonCommand {

    @NotNull
    private ${className}DTO ${classname}DTO;
}

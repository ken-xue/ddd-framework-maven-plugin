package ${basePackageName}.domain.factory.${moduleName};

import ${basePackageName}.domain.domain.${moduleName}.${className};
/**
 * ${comments}
 * @author ${author}
 * @date ${datetime}
 */
public class ${className}Factory {
    public static ${className} get${className}(){
        return new ${className}();
    }
}

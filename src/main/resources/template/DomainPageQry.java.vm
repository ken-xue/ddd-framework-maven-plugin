package ${basePackageName}.coreclient.dto.${moduleName}.${allLowName};

import ${basePackageName}.coreclient.dto.common.command.PageQuery;
import lombok.Data;
import lombok.experimental.Accessors;

/**
 * ${comments}
 * @author ${author}
 * @date ${datetime}
 */
@Data
@Accessors(chain = true)
public class ${className}PageQry extends PageQuery {

    private ${className}DTO ${classname}DTO;

}

package ${basePackageName}.coreclient.dto.${moduleName}.${allLowName}.event;

import ${basePackageName}.coreclient.dto.common.event.DomainEventI;
import lombok.AllArgsConstructor;
import lombok.Data;

/**
 * ${comments}
 * @author ${author}
 * @date ${datetime}
 */
@Data
@AllArgsConstructor
public class ${className}DeleteEvent implements DomainEventI {
    private String uuid;
}
